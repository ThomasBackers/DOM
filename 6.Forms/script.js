const inputs = document.querySelectorAll("input");
const displayField = document.querySelector("#display-firstname");
const displayFieldContent = displayField.innerHTML;

const displayName = (e) => {
    ;
}

inputs[0].addEventListener("keyup", displayName);